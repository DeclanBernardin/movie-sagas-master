import React, { Component } from 'react';


class Home extends Component {
    // Renders the entire app on the DOM
    render() {
        return (
        <div>

        </div>
        );
    }
}

export default Home;
